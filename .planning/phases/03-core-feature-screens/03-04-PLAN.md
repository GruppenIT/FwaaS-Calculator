---
phase: 03-core-feature-screens
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified: []
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - LIST-01
  - LIST-02
  - LIST-03
  - LIST-04
  - LIST-05

must_haves:
  truths:
    - "All phase success criteria are visually verified by the user"
  artifacts: []
  key_links: []
---

<objective>
Run the full test suite, verify TypeScript compilation, and present a visual checkpoint for the user to verify the dashboard and all three listing pages.

Purpose: Automated checks confirm code correctness; human verification confirms visual quality matches the Stripe-style design spec.
Output: All tests green, TypeScript clean, user approval of visual output.
</objective>

<execution_context>
@C:/Users/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-core-feature-screens/03-CONTEXT.md
@.planning/phases/03-core-feature-screens/03-01-SUMMARY.md
@.planning/phases/03-core-feature-screens/03-02-SUMMARY.md
@.planning/phases/03-core-feature-screens/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite + TypeScript + lint verification</name>
  <files></files>
  <action>
    Run all verification commands in sequence:

    1. `pnpm vitest run packages/app-desktop` — all unit tests including new ones from Plan 01
    2. `npx tsc --noEmit --project packages/app-desktop/tsconfig.json` — TypeScript compilation
    3. Verify no hardcoded hex in modified files:
       `grep -rn '#[0-9a-fA-F]\{3,8\}' packages/app-desktop/src/pages/dashboard/dashboard-page.tsx packages/app-desktop/src/pages/processos/processos-page.tsx packages/app-desktop/src/pages/prazos/prazos-page.tsx packages/app-desktop/src/pages/clientes/clientes-page.tsx packages/app-desktop/src/pages/dashboard/urgency-heat-map.tsx packages/app-desktop/src/pages/prazos/prazo-countdown.tsx packages/app-desktop/src/hooks/use-chart-theme.ts`
    4. Verify no var() in Recharts props:
       `grep -n 'stroke=\|fill=' packages/app-desktop/src/pages/dashboard/dashboard-page.tsx | grep 'var('`
    5. Verify no manual table tags in listings:
       `grep -c '<table\|<thead\|<tbody' packages/app-desktop/src/pages/processos/processos-page.tsx packages/app-desktop/src/pages/prazos/prazos-page.tsx packages/app-desktop/src/pages/clientes/clientes-page.tsx`

    If any check fails, fix the issue before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd C:/temp/FwaaS-Calculator && pnpm vitest run packages/app-desktop 2>&1 | tail -20</automated>
  </verify>
  <done>All tests pass. TypeScript compiles cleanly. No hardcoded hex. No var() in Recharts. No manual table tags in listings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of dashboard and listings</name>
  <files></files>
  <action>
    Present the completed Phase 3 work to the user for visual verification. All automated checks must have passed in Task 1 before presenting this checkpoint.

    What was built:
    - Dashboard with 7 Stripe-style KPI stat cards (3px colored left border, 4+3 grid)
    - 2x2 urgency heat map replacing old prazos list (Fatal/Urgente/Semana/Proximo)
    - Recharts charts with theme-aware colors (toggle dark mode to verify)
    - Processos listing with DataTable, JetBrains Mono CNJ numbers, Badge status pills
    - Prazos listing with DataTable, PrazoCountdown with tier-colored countdown
    - Clientes listing with DataTable, row click navigation

    Verification steps for the user:
    1. Open the dashboard page — verify 7 stat cards in 4+3 grid with colored left borders, verify 2x2 urgency heat map, click a heat map cell to navigate to Prazos with tier filter, toggle dark mode to verify chart colors update
    2. Open Processos listing — verify CNJ in monospace, Badge status pills, row click navigation, action button stopPropagation
    3. Open Prazos listing — verify countdown with tier colors, hover tooltip shows absolute date, CNJ in monospace
    4. Open Clientes listing — verify DataTable with sort headers, row click navigation
    5. Resize window to 1366x768 — verify NO horizontal scrollbar on any listing
    6. Toggle dark mode on each page — verify all colors switch correctly
  </action>
  <verify>User types "approved" or describes issues to fix</verify>
  <done>User approves visual quality of all Phase 3 pages in both light and dark mode at 1366x768.</done>
</task>

</tasks>

<verification>
Phase 3 is complete when:
1. All unit tests pass
2. TypeScript compiles cleanly
3. No hardcoded hex in any modified file
4. No var() strings in Recharts SVG props
5. User approves visual output at checkpoint
</verification>

<success_criteria>
- All automated checks pass
- User approves visual quality of dashboard and all 3 listings
- Dark mode works correctly across all pages
- No horizontal scroll at 1366x768
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-feature-screens/03-04-SUMMARY.md`
</output>
