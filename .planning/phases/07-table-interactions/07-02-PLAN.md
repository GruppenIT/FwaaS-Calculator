---
phase: 07-table-interactions
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - packages/app-desktop/src/pages/processos/processos-page.tsx
  - packages/app-desktop/src/pages/prazos/prazos-page.tsx
  - packages/app-desktop/src/pages/clientes/clientes-page.tsx
autonomous: false
requirements:
  - INT-01
  - INT-02
  - INT-03
  - INT-04
  - INT-05

must_haves:
  truths:
    - "Arrow keys navigate rows in processos, prazos, and clientes tables"
    - "Pressing N on any listing page opens the creation modal (when no input is focused)"
    - "Pressing Esc clears the search input on processos and clientes pages"
    - "Processos and prazos pages show a column visibility toggle button"
    - "Hiding a column in processos persists after page reload"
    - "Sorting a column in processos persists after page reload"
    - "Hovering over clienteNome in processos table shows a hover card with client details"
  artifacts:
    - path: "packages/app-desktop/src/pages/processos/processos-page.tsx"
      provides: "Processos listing with all 5 interactions wired"
      contains: "useTablePreferences"
    - path: "packages/app-desktop/src/pages/prazos/prazos-page.tsx"
      provides: "Prazos listing with keyboard nav, sort persistence, column visibility"
      contains: "useTablePreferences"
    - path: "packages/app-desktop/src/pages/clientes/clientes-page.tsx"
      provides: "Clientes listing with keyboard nav, sort persistence, N shortcut, Esc"
      contains: "useTablePreferences"
  key_links:
    - from: "packages/app-desktop/src/pages/processos/processos-page.tsx"
      to: "packages/app-desktop/src/hooks/use-table-preferences.ts"
      via: "useTablePreferences('processos')"
      pattern: "useTablePreferences.*processos"
    - from: "packages/app-desktop/src/pages/processos/processos-page.tsx"
      to: "packages/app-desktop/src/components/ui/client-hover-card.tsx"
      via: "ClientHoverCard wrapping clienteNome column render"
      pattern: "ClientHoverCard"
    - from: "packages/app-desktop/src/pages/processos/processos-page.tsx"
      to: "packages/app-desktop/src/components/ui/column-visibility-toggle.tsx"
      via: "ColumnVisibilityToggle in filter bar"
      pattern: "ColumnVisibilityToggle"
---

<objective>
Wire all table interaction infrastructure (from Plan 07-01) into the three main listing pages: ProcessosPage, PrazosPage, and ClientesPage. Add page-level keyboard shortcuts (N to create, Esc to clear search).

Purpose: This is the page-level integration that makes all 5 INT requirements user-facing.
Output: All three listing pages fully interactive with keyboard nav, shortcuts, column visibility, hover cards, and persistent sort.
</objective>

<execution_context>
@C:/Users/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-table-interactions/07-01-SUMMARY.md

@packages/app-desktop/src/pages/processos/processos-page.tsx
@packages/app-desktop/src/pages/prazos/prazos-page.tsx
@packages/app-desktop/src/pages/clientes/clientes-page.tsx

<interfaces>
<!-- From Plan 07-01 — contracts the executor uses directly -->

From packages/app-desktop/src/hooks/use-table-preferences.ts:
```typescript
export function useTablePreferences(tableId: string): {
  sortState: { key: string; direction: 'asc' | 'desc' | null } | undefined;
  setSortState: (state: { key: string; direction: 'asc' | 'desc' | null }) => void;
  hiddenColumns: string[];
  toggleColumn: (columnKey: string) => void;
  isColumnVisible: (columnKey: string) => boolean;
}
```

From packages/app-desktop/src/components/ui/data-table.tsx:
```typescript
// New prop added in 07-01:
hiddenColumns?: string[]
// Arrow key nav is automatic when onRowClick is provided — no extra props needed
```

From packages/app-desktop/src/components/ui/column-visibility-toggle.tsx:
```typescript
export function ColumnVisibilityToggle(props: {
  columns: { key: string; header: string }[];
  hiddenColumns: string[];
  onToggle: (columnKey: string) => void;
}): JSX.Element;
```

From packages/app-desktop/src/components/ui/client-hover-card.tsx:
```typescript
export function ClientHoverCard(props: {
  clienteId: string;
  clienteNome: string;
  children: React.ReactNode;
}): JSX.Element;
```

From packages/app-desktop/src/lib/api.ts:
```typescript
export interface ProcessoListRow {
  id: string;
  numeroCnj: string;
  clienteNome: string | null;
  clienteId?: string; // NOTE: check if exists — may need to add to API response
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire interactions into ProcessosPage (all 5 requirements)</name>
  <files>packages/app-desktop/src/pages/processos/processos-page.tsx</files>
  <action>
Modify ProcessosPage to integrate all interaction features. PRESERVE all existing functionality (filters, modal, CSV export, delete confirm).

**1. Replace local sortState with useTablePreferences (INT-05):**
- Import `useTablePreferences` from `../../hooks/use-table-preferences`
- Replace `const [sortState, setSortState] = useState(...)` with `const { sortState, setSortState, hiddenColumns, toggleColumn } = useTablePreferences('processos')`
- Pass `sortState` and `setSortState` to DataTable as before (API unchanged)

**2. Pass hiddenColumns to DataTable (INT-03):**
- Add `hiddenColumns={hiddenColumns}` prop to the DataTable component

**3. Add ColumnVisibilityToggle to filter bar (INT-03):**
- Import `ColumnVisibilityToggle` from `../../components/ui`
- Add `<ColumnVisibilityToggle columns={columns} hiddenColumns={hiddenColumns} onToggle={toggleColumn} />` in the filter bar, between the area select and the clear-filters button
- Pass the columns array with key+header (the component filters out empty headers internally)

**4. Wrap clienteNome in ClientHoverCard (INT-04):**
- Import `ClientHoverCard` from `../../components/ui`
- In the `clienteNome` column render function, check if the ProcessoListRow has a `clienteId` field available. If `ProcessoListRow` does not include `clienteId`, the hover card cannot be wired (skip this — note it in summary). If it does include a client ID field:
  - Wrap the name span in `<ClientHoverCard clienteId={row.clienteId} clienteNome={String(value ?? '-')}><span ...>{name}</span></ClientHoverCard>`
- If clienteId is not on ProcessoListRow, check the actual API response by looking at the data. The processos listing may join the client table. If clienteId is not available, add a comment `// TODO: clienteId needed from API for hover card` and render the name normally. Document in summary.

**5. Add keyboard shortcuts (INT-01 Esc, INT-02 N):**
- Add a `useEffect` with a `keydown` listener on `document`:
  - `N` key (when `document.activeElement` is `document.body` or a `<tr>` — NOT an input/select/textarea): call `setModalData(null)` to open the "Novo processo" modal. Only if `can('processos:criar')`.
  - `Escape` key (when the search input has value): clear `busca` to empty string. Use a `useRef` on the search input to also call `inputRef.current?.blur()`.
- Guard against firing when a modal is open: check `!showModal` before processing shortcuts.
- Cleanup the event listener in useEffect return.

**Arrow key nav (INT-01):** Already handled by DataTable internally from Plan 07-01 — no page changes needed.
  </action>
  <verify>
    <automated>cd C:/temp/FwaaS-Calculator && cd packages/app-desktop && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>ProcessosPage uses useTablePreferences for persistent sort, shows ColumnVisibilityToggle, has N shortcut opening create modal, Esc clears search, and (if clienteId available) clienteNome shows hover card.</done>
</task>

<task type="auto">
  <name>Task 2: Wire interactions into PrazosPage and ClientesPage</name>
  <files>packages/app-desktop/src/pages/prazos/prazos-page.tsx, packages/app-desktop/src/pages/clientes/clientes-page.tsx</files>
  <action>
Apply the same interaction pattern to PrazosPage and ClientesPage.

**PrazosPage:**
- Replace local sortState with `useTablePreferences('prazos')` — destructure sortState, setSortState, hiddenColumns, toggleColumn
- Pass `hiddenColumns={hiddenColumns}` to DataTable
- Add `<ColumnVisibilityToggle columns={columns} hiddenColumns={hiddenColumns} onToggle={toggleColumn} />` after the status filter buttons, in a flex container
- Add document keydown listener:
  - `N`: open create modal via `setModalData(null)` (when not in input, not in modal, and `can('processos:editar')` — same permission check used for the button)
  - `Escape`: no search input on this page, so no Esc handler needed
- Arrow key nav is automatic from DataTable.

**ClientesPage:**
- Replace local sortState with `useTablePreferences('clientes')` — destructure sortState, setSortState (no column visibility toggle for clientes per requirements — INT-03 only specifies processos and prazos)
- Pass sortState and setSortState to DataTable as before
- Do NOT add ColumnVisibilityToggle or hiddenColumns to ClientesPage (out of scope per INT-03)
- Add document keydown listener:
  - `N`: open create modal via `setModalData(null)` (when not in input, not in modal, `can('clientes:criar')`)
  - `Escape`: clear `busca` state and blur the search input (same pattern as ProcessosPage)
- Add `useRef` to the search input for Esc blur.
- Arrow key nav is automatic from DataTable.
  </action>
  <verify>
    <automated>cd C:/temp/FwaaS-Calculator && cd packages/app-desktop && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>PrazosPage has persistent sort, column visibility toggle, and N shortcut. ClientesPage has persistent sort, N shortcut, and Esc to clear search. Both have arrow key row navigation via DataTable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full table interactions across all listing pages: arrow key row navigation, N shortcut for new record, Esc to clear search, column visibility toggles (processos + prazos), hover card on client name (processos), and sort persistence across page reloads.</what-built>
  <how-to-verify>
    1. Start the app: `cd packages/app-desktop && pnpm dev`
    2. **Processos page (#/app/processos):**
       - Click a table row, then press ArrowDown/ArrowUp — focus should move between rows
       - Press Enter on a focused row — should navigate to processo detail
       - Press N (not in search input) — "Novo processo" modal should open. Close it.
       - Type in search box, then press Escape — search should clear
       - Sort by "Numero CNJ" column, navigate away (#/app/clientes), come back — sort should persist
       - Click the Columns button — toggle a column off, reload page — column should stay hidden
       - Hover over a client name — hover card should appear with client details (if clienteId was available)
    3. **Prazos page (#/app/prazos):**
       - Arrow keys navigate rows
       - Press N — "Novo prazo" modal opens
       - Column visibility toggle works (hide/show columns)
       - Sort persists across navigation
    4. **Clientes page (#/app/clientes):**
       - Arrow keys navigate rows
       - Press N — "Novo cliente" modal opens
       - Esc clears search
       - Sort persists across navigation
       - No column visibility toggle (by design)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `cd packages/app-desktop && npx tsc --noEmit` passes
2. All 5 INT requirements are addressed:
   - INT-01: Arrow keys + Enter in all tables, Esc clears search in processos/clientes
   - INT-02: N shortcut opens create modal on all listing pages
   - INT-03: Column visibility toggle on processos and prazos, persisted in localStorage
   - INT-04: Client hover card on processos table clienteNome column
   - INT-05: Sort state persisted in localStorage per table
3. Human verification confirms all interactions work visually
</verification>

<success_criteria>
- All 3 listing pages use useTablePreferences for persistent sort
- Processos and prazos have column visibility toggles
- N shortcut works on all listing pages
- Esc clears search on processos and clientes
- Arrow keys navigate table rows on all pages
- Client hover card shows on processos clienteNome hover
- Preferences survive page reload (localStorage)
</success_criteria>

<output>
After completion, create `.planning/phases/07-table-interactions/07-02-SUMMARY.md`
</output>
