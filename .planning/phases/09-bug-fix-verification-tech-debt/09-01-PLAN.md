---
phase: 09-bug-fix-verification-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/app-desktop/src/pages/prazos/prazos-page.tsx
  - packages/database/src/api-server.ts
  - packages/app-desktop/src/components/ui/index.ts
autonomous: true
requirements: [INT-02, VIS-01, VIS-03]
gap_closure: true

must_haves:
  truths:
    - "PrazosPage N shortcut checks prazos permission, not processos permission"
    - "PrazosPage header + button checks prazos permission, not processos permission"
    - "prazosFatais returns actual count of fatal pending prazos, not hardcoded 0"
    - "Sparkline and ProcessoTimeline are exported from UI barrel index.ts"
  artifacts:
    - path: "packages/app-desktop/src/pages/prazos/prazos-page.tsx"
      provides: "Corrected permission guards for prazos"
      contains: "prazos:criar"
    - path: "packages/database/src/api-server.ts"
      provides: "Real prazosFatais query"
      contains: "prazosFatais"
    - path: "packages/app-desktop/src/components/ui/index.ts"
      provides: "Barrel exports for Sparkline and ProcessoTimeline"
      contains: "Sparkline"
  key_links:
    - from: "prazos-page.tsx"
      to: "auth/permissions"
      via: "can('prazos:criar') || can('prazos:editar')"
      pattern: "prazos:(criar|editar)"
    - from: "api-server.ts"
      to: "s.prazos"
      via: "Drizzle count query with fatal + pendente filter"
      pattern: "prazos\\.fatal.*pendente|pendente.*prazos\\.fatal"
---

<objective>
Fix the PrazosPage permission guard copy-paste bug, implement the prazosFatais actual query, and export missing UI components from the barrel index.

Purpose: Close 3 gaps from the v2.1 milestone audit — a permission bug (INT-02), hardcoded prazosFatais (tech debt), and missing barrel exports (style consistency).
Output: Corrected prazos-page.tsx, api-server.ts with real fatal prazo count, updated UI index.ts.
</objective>

<execution_context>
@C:/Users/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v2.1-MILESTONE-AUDIT.md

<interfaces>
<!-- Permission guard pattern from ProcessosPage (correct reference): -->
<!-- PrazosPage lines 131 and 313 incorrectly use can('processos:editar') -->
<!-- Should use can('prazos:criar') || can('prazos:editar') like the prazos edit button at line 284 -->

From packages/database/src/api-server.ts (prazosPendentes query pattern to follow):
```typescript
const [prazosPendentes] = await dbq
  .select({ count: count() })
  .from(s.prazos)
  .where(eq(s.prazos.status, 'pendente'));
```
<!-- prazosFatais should follow same pattern but add: and(eq(s.prazos.status, 'pendente'), eq(s.prazos.fatal, true)) -->

From packages/app-desktop/src/components/ui/index.ts (current exports, missing Sparkline and ProcessoTimeline):
```typescript
export { ColumnVisibilityToggle } from './column-visibility-toggle';
export { ClientHoverCard } from './client-hover-card';
// Missing: export { Sparkline } from './sparkline';
// Missing: export { ProcessoTimeline } from './processo-timeline';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix PrazosPage permission bug and add UI barrel exports</name>
  <files>packages/app-desktop/src/pages/prazos/prazos-page.tsx, packages/app-desktop/src/components/ui/index.ts</files>
  <action>
In prazos-page.tsx, fix the copy-paste permission bug at two locations:

1. Line 131: Change `if (can('processos:editar'))` to `if (can('prazos:criar') || can('prazos:editar'))` — this is the N keyboard shortcut handler.

2. Line 313: Change `can('processos:editar') ? (` to `(can('prazos:criar') || can('prazos:editar')) ? (` — this is the PageHeader action button guard.

Do NOT change line 284 which already correctly uses `can('prazos:editar')`.

In index.ts, add two exports at the end of the file:
```typescript
export { Sparkline } from './sparkline';
export { ProcessoTimeline } from './processo-timeline';
```
  </action>
  <verify>
    <automated>cd C:/temp/FwaaS-Calculator && grep -n "processos:editar" packages/app-desktop/src/pages/prazos/prazos-page.tsx | grep -v "//"; grep -n "Sparkline\|ProcessoTimeline" packages/app-desktop/src/components/ui/index.ts</automated>
  </verify>
  <done>prazos-page.tsx has zero occurrences of 'processos:editar' (only prazos:criar/prazos:editar). UI index.ts exports both Sparkline and ProcessoTimeline.</done>
</task>

<task type="auto">
  <name>Task 2: Implement real prazosFatais query in api-server.ts</name>
  <files>packages/database/src/api-server.ts</files>
  <action>
In api-server.ts, add a prazosFatais count query after the existing prazosPendentes query (around line 2506). Follow the same pattern:

```typescript
const [prazosFataisCount] = await dbq
  .select({ count: count() })
  .from(s.prazos)
  .where(and(eq(s.prazos.status, 'pendente'), eq(s.prazos.fatal, true)));
```

Then replace the two hardcoded `prazosFatais: 0` usages:

1. Line ~2589 (auto-snapshot insert): Change `prazosFatais: 0` to `prazosFatais: Number(prazosFataisCount?.count ?? 0)`
2. Line ~2603 (dashboard stats response): Change `prazosFatais: 0` to `prazosFatais: prazosFataisCount?.count ?? 0`

The `and` function is already imported (used in other queries in the same file). The `s.prazos.fatal` column exists (confirmed in schema and used in other queries at lines 2764, 2854).
  </action>
  <verify>
    <automated>cd C:/temp/FwaaS-Calculator && grep -n "prazosFatais" packages/database/src/api-server.ts</automated>
  </verify>
  <done>prazosFatais uses real count query with fatal=true AND status=pendente filter. No hardcoded 0 values remain for prazosFatais.</done>
</task>

</tasks>

<verification>
1. `grep -n "processos:editar" packages/app-desktop/src/pages/prazos/prazos-page.tsx` returns empty — no wrong permission references remain
2. `grep -n "prazos:criar" packages/app-desktop/src/pages/prazos/prazos-page.tsx` returns 2 matches (N shortcut + header button)
3. `grep -c "prazosFatais: 0" packages/database/src/api-server.ts` returns 0 — no hardcoded zeros
4. `grep "Sparkline\|ProcessoTimeline" packages/app-desktop/src/components/ui/index.ts` returns 2 export lines
</verification>

<success_criteria>
- PrazosPage N shortcut and header button guard use prazos permissions (not processos)
- prazosFatais returns real count from database query
- Sparkline and ProcessoTimeline exported from UI barrel
</success_criteria>

<output>
After completion, create `.planning/phases/09-bug-fix-verification-tech-debt/09-01-SUMMARY.md`
</output>
