---
phase: 04-detail-pages-auth-e-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/app-desktop/src/pages/login/login-page.tsx
  - packages/app-desktop/electron/splash/splash.html
autonomous: true
requirements: [AUTH-01, AUTH-02]
must_haves:
  truths:
    - "Login page shows split-panel layout: dark left panel with branding + right panel with form"
    - "Left panel has #0F1829 background, CAUSA logo in white, tagline in Lora, feature bullets, version"
    - "Right panel has theme toggle top-right, Entrar heading, form with email/password, submit button"
    - "Splash screen shows Lora 16px tagline instead of Inter 15px"
    - "Splash screen visually cohesifies with login left panel aesthetic"
  artifacts:
    - path: "packages/app-desktop/src/pages/login/login-page.tsx"
      provides: "Split-panel login layout"
      contains: "0F1829"
    - path: "packages/app-desktop/electron/splash/splash.html"
      provides: "Refreshed splash screen with Lora tagline"
      contains: "Lora"
  key_links:
    - from: "login-page.tsx left panel"
      to: "CausaLogo component"
      via: "white variant rendering"
      pattern: "CausaLogo"
    - from: "login-page.tsx form"
      to: "useAuth().login"
      via: "existing handleSubmit"
      pattern: "handleSubmit"
---

<objective>
Redesign the login page to split-panel layout and refresh the splash screen to visually match.

Purpose: AUTH-01 requires a Vercel/Linear-inspired split-panel login. AUTH-02 requires the splash screen to use Lora 16px for the tagline and visually align with the new login left panel.
Output: Rewritten login-page.tsx + refreshed splash.html.
</objective>

<execution_context>
@C:/Users/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-detail-pages-auth-e-polish/04-CONTEXT.md
@.planning/phases/04-detail-pages-auth-e-polish/04-RESEARCH.md

@packages/app-desktop/src/pages/login/login-page.tsx
@packages/app-desktop/electron/splash/splash.html
@packages/app-desktop/src/components/ui/causa-logo.tsx

<interfaces>
From login-page.tsx (existing hooks to preserve):
```typescript
import { useTheme } from '../../hooks/use-theme';
import { useAuth } from '../../lib/auth-context';
const { theme, toggleTheme } = useTheme();
const { login } = useAuth();
```

From causa-logo.tsx (className prop supported):
```typescript
export function CausaLogo({ size, showText, className }: CausaLogoProps)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite login page to split-panel layout</name>
  <files>packages/app-desktop/src/pages/login/login-page.tsx</files>
  <action>
Rewrite login-page.tsx from centered card layout to split-panel layout per AUTH-01:

**Left Panel (always dark, ignores theme):**
- Full height, `w-1/2` on large screens, `hidden lg:flex` (though app is desktop-only at min 1366px, so always visible)
- Background: `style={{ backgroundColor: '#0F1829' }}` — inline style, NOT Tailwind arbitrary value. This is the one justified hardcoded hex in React components per design decision.
- Flex column with `justify-between` and `p-10`
- Top: `<CausaLogo size={48} showText className="[&_img]:brightness-0 [&_img]:invert [&_span]:!text-white" />`
- Center (flex-1 flex-col justify-center gap-6):
  - Tagline: `<p className="font-[var(--font-brand)]" style={{ fontSize: '20px', color: 'white' }}>A sua causa, no seu escritorio.</p>`
  - Feature bullets as `<ul className="space-y-3 text-sm-causa" style={{ color: 'rgba(255,255,255,0.7)' }}>`:
    - "Gestao de prazos e processos"
    - "Controle financeiro completo"
    - "Monitoramento processual automatico"
  - Use em-dash prefix for each bullet: `— Gestao de prazos e processos`
- Bottom: `<p style={{ color: 'rgba(255,255,255,0.3)' }} className="text-xs-causa">CAUSA {appVersion}</p>`

**Right Panel (respects theme):**
- `flex-1 flex flex-col items-center justify-center p-8 bg-[var(--color-bg)] relative`
- Theme toggle button: `absolute top-4 right-4` — keep existing toggle code (Moon/Sun icons)
- Login form card: Keep the existing form structure (email, password, error, submit button) but remove the outer card wrapper since the right panel IS the container. Keep max-w-sm for form width.
- Heading: "Entrar" (h2)
- Subtitle: "Acesse seu escritorio" (p)

**Preserve all existing logic:** useState for email/senha/error/loading, useEffect for appVersion, handleSubmit with login(), error display. Do NOT change form behavior.

AVOID: Do NOT use `var(--color-bg)` for the left panel background. Do NOT use Tailwind bg-[] for the left panel. Left panel colors are hardcoded white-on-dark, theme-independent.
  </action>
  <verify>
    <automated>cd C:/temp/FwaaS-Calculator && npx tsc --noEmit --project packages/app-desktop/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
- Login page renders split-panel: dark left (#0F1829) + theme-aware right
- Left panel shows CAUSA logo (white), tagline in Lora, 3 feature bullets, version
- Right panel shows theme toggle (top-right), "Entrar" heading, email/password form
- All existing login logic (auth, error handling, loading state) preserved
- Left panel does NOT change when theme toggles
- TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Refresh splash screen to match login aesthetic</name>
  <files>packages/app-desktop/electron/splash/splash.html</files>
  <action>
Update splash.html to visually cohesify with the new login left panel per AUTH-02:

1. First, read the existing splash.html to understand current structure and font loading.

2. Fix tagline font: Change from Inter 15px to Lora 16px. Check if Lora-Regular.woff2 (weight 400) exists in the splash fonts directory. If only Lora-SemiBold.woff2 (weight 600) exists, either:
   - Add a @font-face for Lora-Regular.woff2 by copying from @fontsource/lora package, OR
   - Use weight 600 (SemiBold) at 16px — acceptable per research.

3. Refresh spacing and visual alignment:
   - Background should already be #0F1829 (verify, keep if correct)
   - Logo should be white and centered
   - Tagline "A sua causa, no seu escritorio" in Lora 16px, centered below logo
   - Progress bar: 2px height, blue (#2563A8 — the primary color), centered near bottom
   - Version text at bottom in small white text with low opacity

4. Keep ALL existing Electron IPC integration intact — the progress bar and loading messages are driven by IPC from the main process. Do NOT modify JavaScript logic.

AVOID: Do NOT change the IPC message handling. Do NOT change the progress bar JavaScript logic. Only update CSS styling and font references.
  </action>
  <verify>
    <automated>cd C:/temp/FwaaS-Calculator && test -f packages/app-desktop/electron/splash/splash.html && echo "splash.html exists" || echo "MISSING"</automated>
  </verify>
  <done>
- Splash screen tagline uses Lora font at 16px (not Inter 15px)
- Background is #0F1829
- Visual styling aligns with login left panel aesthetic
- Progress bar is 2px blue (#2563A8)
- All Electron IPC integration preserved
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for app-desktop package
- Login page renders split-panel correctly
- Splash.html has Lora font reference for tagline
</verification>

<success_criteria>
- Login page is split-panel with dark left branding and themed right form
- Splash screen tagline is in Lora 16px
- Both screens share the same #0F1829 dark brand aesthetic
- All auth logic and IPC integration preserved
</success_criteria>

<output>
After completion, create `.planning/phases/04-detail-pages-auth-e-polish/04-02-SUMMARY.md`
</output>
