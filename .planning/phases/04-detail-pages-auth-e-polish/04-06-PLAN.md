---
phase: 04-detail-pages-auth-e-polish
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/app-desktop/src/components/layout/sidebar.tsx
  - packages/app-desktop/src/pages/processos/processos-page.tsx
  - packages/app-desktop/src/pages/clientes/clientes-page.tsx
  - packages/app-desktop/src/pages/prazos/prazos-page.tsx
autonomous: true
gap_closure: true
requirements: [DET-03, ANIM-03]
must_haves:
  truths:
    - "Printing processo detail page hides the sidebar completely, no blank column from the aside wrapper"
    - "Table rows in processos, clientes, and prazos listings stagger-animate on first page load with 20ms delay per row (cap 10)"
    - "Sorting or filtering after initial load shows rows instantly without stagger animation"
  artifacts:
    - path: "packages/app-desktop/src/components/layout/sidebar.tsx"
      provides: "data-sidebar attribute on root aside element"
      contains: "data-sidebar"
    - path: "packages/app-desktop/src/pages/processos/processos-page.tsx"
      provides: "animateFirstLoad prop passed to DataTable"
      contains: "animateFirstLoad"
    - path: "packages/app-desktop/src/pages/clientes/clientes-page.tsx"
      provides: "animateFirstLoad prop passed to DataTable"
      contains: "animateFirstLoad"
    - path: "packages/app-desktop/src/pages/prazos/prazos-page.tsx"
      provides: "animateFirstLoad prop passed to DataTable"
      contains: "animateFirstLoad"
  key_links:
    - from: "packages/app-desktop/src/styles/globals.css"
      to: "packages/app-desktop/src/components/layout/sidebar.tsx"
      via: "media print data-sidebar display none important"
      pattern: "data-sidebar"
    - from: "packages/app-desktop/src/pages/processos/processos-page.tsx"
      to: "packages/app-desktop/src/components/ui/data-table.tsx"
      via: "animateFirstLoad prop"
      pattern: "animateFirstLoad"
---

<objective>
Close two verification gaps from 04-VERIFICATION.md that block full Phase 4 goal achievement.

Purpose: DET-03 print sidebar hiding and ANIM-03 row stagger wiring are both implemented as infrastructure but never connected to their consumers. This plan wires both.
Output: sidebar.tsx gets data-sidebar attribute; three listing pages pass animateFirstLoad to DataTable.
</objective>

<execution_context>
@C:/Users/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-detail-pages-auth-e-polish/04-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data-sidebar attribute to sidebar aside element</name>
  <files>packages/app-desktop/src/components/layout/sidebar.tsx</files>
  <action>
At line 159 of sidebar.tsx, the root aside element is:
```
<aside className="w-[var(--sidebar-width)] h-screen bg-[var(--color-surface)] border-r border-[var(--color-border)] flex flex-col shrink-0">
```

Add `data-sidebar` attribute to this element:
```
<aside data-sidebar className="w-[var(--sidebar-width)] h-screen bg-[var(--color-surface)] border-r border-[var(--color-border)] flex flex-col shrink-0">
```

This fulfills the CSS contract in globals.css which has `[data-sidebar] { display: none !important }` inside `@media print`. The contract was documented in CSS comments in plan 04-03 but the attribute was never added to the actual element.

No other changes to sidebar.tsx.
  </action>
  <verify>
    <automated>cd C:/temp/FwaaS-Calculator && grep -n "data-sidebar" packages/app-desktop/src/components/layout/sidebar.tsx && grep -n "data-sidebar" packages/app-desktop/src/styles/globals.css</automated>
  </verify>
  <done>sidebar.tsx root aside has data-sidebar attribute; globals.css @media print [data-sidebar] selector now has a matching DOM element</done>
</task>

<task type="auto">
  <name>Task 2: Wire animateFirstLoad prop to all listing page DataTables</name>
  <files>packages/app-desktop/src/pages/processos/processos-page.tsx, packages/app-desktop/src/pages/clientes/clientes-page.tsx, packages/app-desktop/src/pages/prazos/prazos-page.tsx</files>
  <action>
In each of the three listing pages, add isFirstLoad state and pass it to DataTable:

**Pattern for all three pages (same approach):**

1. Add state: `const [isFirstLoad, setIsFirstLoad] = useState(true);`
2. In the `carregar` callback's `finally` block (after `setLoading(false)`), add: `setIsFirstLoad(false);`
   - IMPORTANT: Only set false on the FIRST successful load. Use a ref or check current value: `setIsFirstLoad(false)` is idempotent so calling it on every carregar is fine — after the first call it stays false.
3. On the DataTable JSX, add prop: `animateFirstLoad={isFirstLoad}`

**processos-page.tsx specifics:**
- Line ~84: `carregar` callback has `finally { setLoading(false); }` — add `setIsFirstLoad(false);` after setLoading
- Line ~362: DataTable usage — add `animateFirstLoad={isFirstLoad}` prop
- Note: carregar is called on busca changes (debounced). isFirstLoad will be false after first call, so subsequent search-triggered calls pass false. This is correct behavior.

**clientes-page.tsx specifics:**
- Same pattern: find carregar's finally block, add setIsFirstLoad(false)
- Find DataTable usage, add animateFirstLoad={isFirstLoad}

**prazos-page.tsx specifics:**
- Same pattern: find carregar's finally block, add setIsFirstLoad(false)
- Find DataTable usage, add animateFirstLoad={isFirstLoad}

Per user decision: "animateFirstLoad is a controlled prop from parent, not internal DataTable state — parent manages when to animate". The DataTable already has the prop defined and the rowFadeIn CSS keyframe exists in globals.css. This task only wires the consumers.

Do NOT modify data-table.tsx — the infrastructure is already complete there.
  </action>
  <verify>
    <automated>cd C:/temp/FwaaS-Calculator && grep -n "animateFirstLoad" packages/app-desktop/src/pages/processos/processos-page.tsx packages/app-desktop/src/pages/clientes/clientes-page.tsx packages/app-desktop/src/pages/prazos/prazos-page.tsx && npx tsc --noEmit --project packages/app-desktop/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>All three listing pages pass animateFirstLoad={isFirstLoad} to DataTable. isFirstLoad starts true and flips to false after first data load. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
1. `grep -rn "data-sidebar" packages/app-desktop/src/components/layout/sidebar.tsx` — confirms attribute present
2. `grep -rn "animateFirstLoad" packages/app-desktop/src/pages/` — confirms prop wired in all 3 pages
3. `npx tsc --noEmit --project packages/app-desktop/tsconfig.json` — no type errors
4. Human verification (from 04-VERIFICATION.md):
   - Ctrl+P on processo detail page: no sidebar column visible in print preview
   - Navigate to processos listing: rows stagger-animate on first load, then sort — rows appear instantly
</verification>

<success_criteria>
- sidebar.tsx aside element has data-sidebar attribute matching globals.css @media print selector
- processos-page.tsx, clientes-page.tsx, prazos-page.tsx all pass animateFirstLoad to DataTable
- TypeScript compiles cleanly
- Both VERIFICATION.md gaps (DET-03 print sidebar, ANIM-03 row stagger) are resolved
</success_criteria>

<output>
After completion, create `.planning/phases/04-detail-pages-auth-e-polish/04-06-SUMMARY.md`
</output>
