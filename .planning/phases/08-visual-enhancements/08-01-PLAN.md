---
phase: 08-visual-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/app-desktop/src/hooks/use-sidebar.ts
  - packages/app-desktop/src/components/layout/sidebar.tsx
  - packages/app-desktop/src/components/layout/app-layout.tsx
  - packages/app-desktop/src/styles/globals.css
autonomous: true
requirements: [VIS-01]

must_haves:
  truths:
    - "On screens 1024px or narrower, sidebar automatically collapses to icon rail showing only icons"
    - "On screens wider than 1024px, sidebar is expanded by default"
    - "User can click a toggle button to collapse/expand sidebar manually on wide screens"
    - "Collapsed sidebar shows icons only (no labels, no section titles) at 64px width"
    - "Expanded sidebar shows full labels at 240px width as before"
    - "Navigation still works correctly when sidebar is collapsed"
    - "User info section hides text when collapsed, shows only avatar"
    - "Theme toggle and logout remain accessible when collapsed"
  artifacts:
    - path: "packages/app-desktop/src/hooks/use-sidebar.ts"
      provides: "Sidebar collapse state management with media query"
      exports: ["useSidebar", "SidebarProvider"]
    - path: "packages/app-desktop/src/components/layout/sidebar.tsx"
      provides: "Collapsible sidebar with icon rail mode"
      min_lines: 100
    - path: "packages/app-desktop/src/components/layout/app-layout.tsx"
      provides: "Layout that adapts to sidebar collapse state"
  key_links:
    - from: "packages/app-desktop/src/hooks/use-sidebar.ts"
      to: "window.matchMedia"
      via: "media query listener for (max-width: 1024px)"
      pattern: "matchMedia.*1024"
    - from: "packages/app-desktop/src/components/layout/sidebar.tsx"
      to: "packages/app-desktop/src/hooks/use-sidebar.ts"
      via: "useSidebar hook"
      pattern: "useSidebar"
    - from: "packages/app-desktop/src/components/layout/app-layout.tsx"
      to: "packages/app-desktop/src/hooks/use-sidebar.ts"
      via: "SidebarProvider wrapping layout"
      pattern: "SidebarProvider"
---

<objective>
Implement a collapsible sidebar that auto-collapses to an icon rail on screens <= 1024px and supports manual toggle on wider screens.

Purpose: Lawyers working on smaller screens (laptops, courtroom tablets) get more usable screen real estate without losing navigation access.
Output: Responsive sidebar with icon rail mode, useSidebar hook, updated layout.
</objective>

<execution_context>
@C:/Users/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing sidebar structure the executor needs -->

From packages/app-desktop/src/components/layout/sidebar.tsx:
```typescript
interface NavItem {
  to: string;
  icon: typeof LayoutDashboard;
  label: string;
  permissions?: string[];
  featureFlag?: keyof AppFeatures;
}

interface NavSection {
  title: string;
  items: NavItem[];
}

// Current sidebar is 240px fixed width via CSS var --sidebar-width
// aside uses data-sidebar attribute, w-[var(--sidebar-width)]
```

From packages/app-desktop/src/components/layout/app-layout.tsx:
```typescript
// Layout is flex h-screen with <Sidebar /> + main content area
// Sidebar is a direct child of the flex container
```

From packages/app-desktop/src/styles/globals.css:
```css
--sidebar-width: 240px; /* line 70 */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useSidebar hook and SidebarProvider</name>
  <files>packages/app-desktop/src/hooks/use-sidebar.ts</files>
  <action>
Create a React context + hook for sidebar collapse state:

1. Create `SidebarProvider` context that manages:
   - `collapsed: boolean` — whether sidebar is in icon-rail mode
   - `toggle: () => void` — manually toggle collapsed state
   - `autoCollapsed: boolean` — whether collapse was triggered by media query (not user)

2. Logic:
   - Use `window.matchMedia('(max-width: 1024px)')` to detect narrow screens
   - On narrow screens (<= 1024px): auto-collapse, `autoCollapsed = true`
   - On wide screens (> 1024px): default expanded, user can toggle manually
   - When screen resizes from narrow to wide: restore to expanded (unless user manually collapsed)
   - When screen resizes from wide to narrow: force collapse regardless of user preference
   - Store manual preference in localStorage key `causa-sidebar-collapsed` (only for wide screens)

3. Export `useSidebar()` hook that reads context (throws if used outside provider).

Follow existing hook patterns: file named `use-sidebar.ts`, kebab-case. Use `useEffect` + `addEventListener('change', ...)` on the MediaQueryList for resize detection. Do NOT use `window.addEventListener('resize')` — MediaQueryList change event is more performant.
  </action>
  <verify>
    <automated>cd C:/temp/FwaaS-Calculator && npx tsc --noEmit --project packages/app-desktop/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>useSidebar hook exists, TypeScript compiles, provides collapsed state + toggle + auto-detection</done>
</task>

<task type="auto">
  <name>Task 2: Update Sidebar and AppLayout for collapse behavior</name>
  <files>packages/app-desktop/src/components/layout/sidebar.tsx, packages/app-desktop/src/components/layout/app-layout.tsx, packages/app-desktop/src/styles/globals.css</files>
  <action>
**globals.css changes:**
- Add `--sidebar-width-collapsed: 64px;` CSS variable next to existing `--sidebar-width: 240px;`

**app-layout.tsx changes:**
- Import `SidebarProvider` from `../../hooks/use-sidebar`
- Wrap the outermost `<div className="flex h-screen ...">` with `<SidebarProvider>...</SidebarProvider>`
- No other changes needed — Sidebar reads context internally

**sidebar.tsx changes:**
- Import `useSidebar` from `../../hooks/use-sidebar`
- Import `PanelLeftClose`, `PanelLeftOpen` from lucide-react (collapse toggle icons)
- Call `const { collapsed, toggle, autoCollapsed } = useSidebar();`

When `collapsed = true` (icon rail mode):
1. `<aside>` width changes from `w-[var(--sidebar-width)]` to `w-[var(--sidebar-width-collapsed)]` — use conditional class
2. Section titles (`GERAL`, `FINANCEIRO`, etc.) are hidden — add `hidden` class when collapsed
3. NavLink labels are hidden — only show `<Icon size={18} />`, no `{label}` text
4. NavLink layout changes from `flex items-center gap-2.5 px-2.5` to `flex items-center justify-center px-0 py-2.5` when collapsed
5. Add `title={label}` attribute to each NavLink when collapsed (native tooltip for icon-only mode)
6. User info section: hide email and role text, show only the initials avatar circle centered
7. Footer buttons: hide "Escuro"/"Claro" text, show only icons centered
8. Logo: replace `<CausaLogo size={32} />` with a smaller version or just the icon mark — use `<CausaLogo size={collapsed ? 24 : 32} />` and remove any text next to it

Toggle button:
- Add a toggle button at the top of the sidebar (below logo, above nav) visible ONLY when `!autoCollapsed` (wide screens)
- Use `PanelLeftClose` icon when expanded, `PanelLeftOpen` when collapsed
- Style: `px-2.5 py-1.5 rounded-[var(--radius-md)] text-[var(--color-text-muted)] hover:bg-causa-surface-alt transition-causa cursor-pointer`
- On narrow screens (`autoCollapsed`), do NOT show toggle — sidebar is always collapsed

Transition: Add `transition-all duration-200` to the `<aside>` for smooth width animation. Also add `overflow-hidden` to prevent text wrapping during transition.

Keep all existing permission filtering, feature flag checks, NavLink active styles intact. The only changes are conditional rendering based on `collapsed` state.
  </action>
  <verify>
    <automated>cd C:/temp/FwaaS-Calculator && npx tsc --noEmit --project packages/app-desktop/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Sidebar collapses to 64px icon rail on screens <= 1024px, toggle button works on wide screens, all nav items accessible via icons + tooltips when collapsed</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors
2. On screens <= 1024px, sidebar shows as 64px icon rail automatically
3. On screens > 1024px, sidebar is 240px by default with a toggle button
4. Clicking toggle collapses to 64px icon rail with smooth animation
5. Navigation links work in both modes
6. Tooltips appear on hover when collapsed
7. Theme toggle and logout work in collapsed mode
</verification>

<success_criteria>
- Sidebar auto-collapses at <= 1024px breakpoint
- Manual toggle works on wider screens
- Collapsed mode shows icons only at 64px width
- Smooth transition animation between states
- All existing sidebar functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/08-visual-enhancements/08-01-SUMMARY.md`
</output>
