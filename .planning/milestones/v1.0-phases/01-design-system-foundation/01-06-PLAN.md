---
phase: 01-design-system-foundation
plan: 06
type: execute
wave: 3
depends_on: [01-02, 01-03, 01-04, 01-05]
files_modified:
  - packages/app-desktop/src/components/ui/index.ts
autonomous: true
requirements: [DS-11, DS-14]

must_haves:
  truths:
    - "All 15 design system components are importable via single barrel export"
    - "Barrel export excludes app-specific components (backup-indicator, causa-logo)"
    - "TypeScript compiles with all imports resolved"
    - "ESLint passes on all component files with no-hardcoded-hex rule active"
  artifacts:
    - path: "packages/app-desktop/src/components/ui/index.ts"
      provides: "Barrel export for all design system primitives"
      exports: ["Button", "Input", "Modal", "ConfirmDialog", "ToastProvider", "useToast", "Skeleton", "SkeletonText", "SkeletonTableRows", "EmptyState", "PageHeader", "Badge", "StatusDot", "Card", "DataTable", "Select", "Checkbox", "Textarea"]
  key_links:
    - from: "index.ts"
      to: "all 15 component files"
      via: "re-export"
      pattern: "export.*from"
---

<objective>
Create barrel export for all design system primitives and run final phase verification.

Purpose: Barrel export (components/ui/index.ts) makes all primitives importable from a single path. Final verification confirms the entire design system is correct.
Output: index.ts barrel export. Full phase passes TypeScript and ESLint.
</objective>

<execution_context>
@C:/Users/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-design-system-foundation/01-CONTEXT.md
@.planning/phases/01-design-system-foundation/01-01-SUMMARY.md
@.planning/phases/01-design-system-foundation/01-02-SUMMARY.md
@.planning/phases/01-design-system-foundation/01-03-SUMMARY.md
@.planning/phases/01-design-system-foundation/01-04-SUMMARY.md
@.planning/phases/01-design-system-foundation/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create barrel export index.ts</name>
  <files>packages/app-desktop/src/components/ui/index.ts</files>
  <action>
Create `packages/app-desktop/src/components/ui/index.ts` with re-exports for ALL design system primitives. Per CONTEXT.md: excludes app-specific components (backup-indicator.tsx, causa-logo.tsx).

```typescript
// Design System Primitives — Barrel Export
// Excludes app-specific: backup-indicator.tsx, causa-logo.tsx

// Existing (refactored)
export { Button } from './button';
export type { ButtonProps } from './button';
export { Input } from './input';
export { Modal } from './modal';
export { ConfirmDialog } from './confirm-dialog';
export { ToastProvider, useToast } from './toast';
export { Skeleton, SkeletonText, SkeletonTableRows } from './skeleton';
export { EmptyState } from './empty-state';
export { PageHeader } from './page-header';

// New primitives
export { Badge } from './badge';
export type { BadgeStatus } from './badge';
export { StatusDot } from './status-dot';
export { Card } from './card';
export { DataTable } from './data-table';
export type { Column } from './data-table';
export { Select } from './select';
export { Checkbox } from './checkbox';
export { Textarea } from './textarea';
```

Also export relevant types that consumers will need: `ButtonProps`, `BadgeStatus`, `Column<T>`.
  </action>
  <verify>
    <automated>cd packages/app-desktop && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>index.ts re-exports all 15 components + key types. TypeScript compiles. backup-indicator and causa-logo are excluded.</done>
</task>

<task type="auto">
  <name>Task 2: Run full phase verification — TypeScript, ESLint, build</name>
  <files></files>
  <action>
Run three verification passes in sequence:

1. **TypeScript compilation:**
   ```bash
   cd packages/app-desktop && npx tsc --noEmit
   ```
   Fix any type errors in component files.

2. **ESLint with no-hardcoded-hex rule:**
   ```bash
   cd packages/app-desktop && npx eslint src/components/ui/
   ```
   Fix any hardcoded hex violations. Violations are expected ONLY if a component accidentally has a hex literal string.

3. **Vite build:**
   ```bash
   cd packages/app-desktop && npx vite build
   ```
   Confirms Fontsource imports resolve, Radix packages resolve, all component imports work.

If any step fails, fix the issue in the relevant file and re-run. Do NOT skip failures.
  </action>
  <verify>
    <automated>cd packages/app-desktop && npx tsc --noEmit && npx vite build 2>&1 | tail -3</automated>
  </verify>
  <done>TypeScript compiles with zero errors. ESLint passes on all component files. Vite build succeeds with all new dependencies resolved.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero errors
2. `npx eslint src/components/ui/` — zero errors (no hardcoded hex)
3. `npx vite build` — build succeeds
4. index.ts file exists with 15+ export lines
5. All requirements DS-01 through DS-19 addressed across plans 01-06
</verification>

<success_criteria>
- Barrel export index.ts exists with all 15 primitives exported
- TypeScript compiles cleanly
- ESLint no-hardcoded-hex rule passes on all components
- Vite build succeeds
- Phase 1 design system is complete and ready for Phase 2 consumption
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-06-SUMMARY.md`
</output>
