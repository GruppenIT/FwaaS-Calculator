---
phase: 09-bug-fix-verification-tech-debt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-table-interactions/07-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [INT-01, INT-03, INT-04, INT-05]
gap_closure: true

must_haves:
  truths:
    - "Phase 7 has a VERIFICATION.md confirming all 5 INT requirements"
    - "REQUIREMENTS.md checkboxes for VIS-01 and VIS-03 are checked"
    - "INT-01 through INT-05 are verified with code evidence"
  artifacts:
    - path: ".planning/phases/07-table-interactions/07-VERIFICATION.md"
      provides: "Formal verification of Phase 7 table interactions"
      min_lines: 80
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated checkboxes for VIS-01 and VIS-03"
      contains: "[x] **VIS-01**"
  key_links:
    - from: "07-VERIFICATION.md"
      to: "Phase 7 source files"
      via: "Code evidence grep patterns"
      pattern: "VERIFIED|SATISFIED"
---

<objective>
Create Phase 7 VERIFICATION.md by inspecting actual source code for all 5 INT requirements, and update REQUIREMENTS.md checkboxes for VIS-01 and VIS-03.

Purpose: Close the verification gap (Phase 7 has no VERIFICATION.md) and fix stale documentation checkboxes.
Output: 07-VERIFICATION.md with code evidence for INT-01..05, updated REQUIREMENTS.md.
</objective>

<execution_context>
@C:/Users/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v2.1-MILESTONE-AUDIT.md
@.planning/phases/07-table-interactions/07-01-SUMMARY.md
@.planning/phases/07-table-interactions/07-02-SUMMARY.md

<interfaces>
<!-- Phase 7 key files to verify against: -->
<!-- INT-01 (keyboard nav): packages/app-desktop/src/components/ui/data-table.tsx (arrow key onKeyDown on tbody) -->
<!-- INT-02 (N shortcut): packages/app-desktop/src/pages/processos/processos-page.tsx, prazos-page.tsx, clientes-page.tsx -->
<!-- INT-03 (column visibility): packages/app-desktop/src/components/ui/column-visibility-toggle.tsx + processos-page.tsx + prazos-page.tsx -->
<!-- INT-04 (hover card): packages/app-desktop/src/components/ui/client-hover-card.tsx + processos-page.tsx -->
<!-- INT-05 (sort persistence): packages/app-desktop/src/hooks/use-table-preferences.ts + all listing pages -->

<!-- Note: INT-02 on PrazosPage has a known bug (wrong permission) being fixed in plan 09-01. -->
<!-- The verification should note this as "PARTIAL — permission bug fixed in plan 09-01" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 7 VERIFICATION.md with code evidence</name>
  <files>.planning/phases/07-table-interactions/07-VERIFICATION.md</files>
  <action>
Inspect the Phase 7 source files to create a formal VERIFICATION.md. Follow the same format as the existing 08-VERIFICATION.md.

For each INT requirement, grep/read the relevant source files and record evidence:

**INT-01 (keyboard nav):** Check data-table.tsx for:
- onKeyDown handler on tbody with ArrowUp/ArrowDown navigation
- Enter key to trigger row click (open detail)
- Esc key behavior (check if search clear is handled — may be in page-level useEffect)
Evidence: line numbers, function names, key handler logic.

**INT-02 (N shortcut):** Check processos-page.tsx, prazos-page.tsx, clientes-page.tsx for:
- useEffect with document keydown listener for 'n'/'N'
- isInput guard (HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement)
- showModal guard to avoid triggering when modal is open
Note: PrazosPage has a known permission bug (processos:editar instead of prazos:criar) — mark as "PARTIAL — permission guard bug, fix in 09-01-PLAN".

**INT-03 (column visibility):** Check column-visibility-toggle.tsx exists with toggle UI, and processos-page.tsx + prazos-page.tsx use it.
- Verify ColumnVisibilityToggle component renders checkboxes
- Verify hiddenColumns flows from useTablePreferences to DataTable
- Verify ClientesPage intentionally has NO toggle (per INT-03 spec: processos and prazos only)

**INT-04 (hover card):** Check client-hover-card.tsx exists and processos-page.tsx uses it.
- Verify Radix HoverCard wrapping
- Verify useRef cache for fetch data
- Verify processos-page.tsx renders ClientHoverCard around client name column

**INT-05 (sort persistence):** Check use-table-preferences.ts for localStorage read/write.
- Verify useState initializer reads from localStorage
- Verify useEffect writes on change
- Verify all three listing pages use useTablePreferences

Write the VERIFICATION.md with:
- Frontmatter (phase, verified date, status, score)
- Observable Truths table (one per INT requirement behavior)
- Required Artifacts table
- Key Link Verification table
- Requirements Coverage table
- Human verification section (for keyboard interaction testing)

Use the 08-VERIFICATION.md format as template but adapted for Phase 7 content.
  </action>
  <verify>
    <automated>cd C:/temp/FwaaS-Calculator && test -f .planning/phases/07-table-interactions/07-VERIFICATION.md && grep -c "VERIFIED\|SATISFIED\|PARTIAL" .planning/phases/07-table-interactions/07-VERIFICATION.md</automated>
  </verify>
  <done>07-VERIFICATION.md exists with code evidence for all 5 INT requirements. Each requirement has VERIFIED/SATISFIED status with line-number evidence from source files. INT-02 notes the PrazosPage permission bug and its fix in plan 09-01.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md checkboxes for VIS-01 and VIS-03</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
In REQUIREMENTS.md, update the two stale checkboxes:

1. Change `- [ ] **VIS-01**:` to `- [x] **VIS-01**:` (sidebar colapsavel — verified in 08-VERIFICATION.md)
2. Change `- [ ] **VIS-03**:` to `- [x] **VIS-03**:` (sparklines — verified in 08-VERIFICATION.md)

Also update the Traceability table:
- INT-01 through INT-05: change Status from "Pending" to "Verified" (or "Complete" depending on plan 09-01 execution)
- VIS-01: change Status from "Pending" to "Complete"
- VIS-03: change Status from "Pending" to "Complete"

Update the Coverage section:
- Satisfied count: 5 -> 12 (all requirements now addressed by gap closure Phase 9)
- Pending: 7 -> 0
  </action>
  <verify>
    <automated>cd C:/temp/FwaaS-Calculator && grep -c "\[x\]" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>VIS-01 and VIS-03 checkboxes are [x]. All INT requirements show verified/complete status in traceability table. Coverage shows 12/12 satisfied.</done>
</task>

</tasks>

<verification>
1. `test -f .planning/phases/07-table-interactions/07-VERIFICATION.md` — file exists
2. `grep "INT-01" .planning/phases/07-table-interactions/07-VERIFICATION.md` — requirement covered
3. `grep "\[x\] \*\*VIS-01\*\*" .planning/REQUIREMENTS.md` — checkbox updated
4. `grep "\[x\] \*\*VIS-03\*\*" .planning/REQUIREMENTS.md` — checkbox updated
</verification>

<success_criteria>
- Phase 7 VERIFICATION.md exists with code evidence for all 5 INT requirements
- REQUIREMENTS.md shows [x] for VIS-01 and VIS-03
- All 12 v2.1 requirements traceable as satisfied/complete
</success_criteria>

<output>
After completion, create `.planning/phases/09-bug-fix-verification-tech-debt/09-02-SUMMARY.md`
</output>
